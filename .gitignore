# Never commit real WiFi credentials
secrets.h

# Never commit real device/org identity or the real card-UID registry —
# only the .example.h versions in firmware/rfid_event_logger/config/ are
# meant to be public.
firmware/rfid_event_logger/config/organization.h
firmware/rfid_event_logger/config/users_private.h
firmware/rfid_event_logger/config/sites.h

# Arduino IDE
*.hex
*.elf
*.bin
build/
.vscode/

# macOS
.DS_Store

# Editor
*.swp
