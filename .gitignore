# Never commit real WiFi credentials
secrets.h

# Never commit real device/org identity or the real card-UID registry —
# only the .example.h versions in config/ are meant to be public.
config/organization.h
config/users_private.h
config/sites.h

# Arduino IDE
*.hex
*.elf
*.bin
build/
.vscode/

# macOS
.DS_Store

# Editor
*.swp
